Drop the Physarum thumbnail here.

Expected file (referenced by src/content/projects/physarum.mdx frontmatter):
  cover.png        (header / card thumbnail, ~1600x900)

A good screenshot is the default Ember preset mid-animation — a glowing
orange/red branching network on black. The standalone app to screenshot
is at /physarum/ (run `npm run dev` and open http://localhost:4321/physarum/).

Once cover.png is here it will appear automatically as:
  - the project card thumbnail on /projects and the homepage
  - the cover image / OG image on /projects/physarum
